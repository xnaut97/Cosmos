# Cosmos Project Rules

- Java 8 compatible only
- Never use Java 9+ APIs
- Never rewrite full files unnecessarily
- Only read files directly related to task
- Preserve Lombok annotations
- Use Maven multi-module structure
- Avoid circular dependencies